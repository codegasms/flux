// reusable frontend js(for interactivity) to be used in specific files
// if some js is being used only once, put it in the ejs file itself
// if pure js logic is being re-used across multiple ejs files, and is independent of any component, they can be placed in this folder
